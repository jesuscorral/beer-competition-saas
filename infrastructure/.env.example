# =============================================================================
# Beer Competition SaaS - Local Development Environment (EXAMPLE)
# =============================================================================
# Copy this file to .env and replace placeholder values with your credentials
# NEVER commit the .env file to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL Configuration
# -----------------------------------------------------------------------------
# Primary database for all services with multi-tenant data isolation

POSTGRES_USER=<your-postgres-username>
POSTGRES_PASSWORD=<your-secure-password>
POSTGRES_DB=beer_competition
POSTGRES_PORT=5432

# -----------------------------------------------------------------------------
# pgAdmin Configuration
# -----------------------------------------------------------------------------
# Web-based PostgreSQL management interface
# Access at: http://localhost:5050
PGADMIN_EMAIL=<your-email@example.com>
PGADMIN_PASSWORD=<your-pgadmin-password>
PGADMIN_PORT=5050

# -----------------------------------------------------------------------------
# RabbitMQ Configuration
# -----------------------------------------------------------------------------
# Message broker for event-driven architecture
# Management UI: http://localhost:15672
RABBITMQ_USER=<your-rabbitmq-username>
RABBITMQ_PASSWORD=<your-secure-password>
RABBITMQ_VHOST=/
RABBITMQ_PORT=5672
RABBITMQ_MGMT_PORT=15672

# -----------------------------------------------------------------------------
# Redis Configuration
# -----------------------------------------------------------------------------
# Distributed cache for session management and rate limiting
REDIS_PASSWORD=<your-secure-password>
REDIS_PORT=6379

# -----------------------------------------------------------------------------
# Keycloak Configuration
# -----------------------------------------------------------------------------
# OpenID Connect authentication provider
# Admin console: http://localhost:8080/admin
KEYCLOAK_ADMIN=<your-admin-username>
KEYCLOAK_ADMIN_PASSWORD=<your-secure-password>
KEYCLOAK_PORT=8080

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
ASPNETCORE_ENVIRONMENT=Development
LOG_LEVEL=Debug

# Connection Strings (used by .NET applications)
# These are automatically constructed from the variables above
# Format: Host=postgres;Port=5432;Database=beer_competition;Username=dev_user;Password=dev_password
